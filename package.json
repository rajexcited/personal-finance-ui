{
  "name": "ui-testing",
  "version": "1.0.0",
  "description": "Here, we are organizing test cases and relavant details. the all test cases are functional. the cosmetic issue fixes don't get recorded in test case, but screenshot will give away some idea on site UX.",
  "scripts": {
    "prebuild": "ts-node cypress/helpers/build-idb-injector.ts",
    "build": "tsc --noEmit",
    "fix-latest-report": "ts-node scripts/fix-latest-report.ts",
    "pretests:regression:local": "npm run build",
    "tests:regression:local": "npm-run-all --continue-on-error -s tests:regression:local:run fix-latest-report",
    "tests:regression:local:run": "cross-env NODE_ENV=local cypress run --browser chrome --env grepTags=regression",
    "pretests:regression:dev": "npm run build",
    "tests:regression:dev": "npm-run-all --continue-on-error -s tests:regression:dev:run fix-latest-report",
    "tests:regression:dev:run": "cross-env NODE_ENV=development cypress run --browser chrome --env grepTags=regression",
    "pretests:regression:experiment": "npm run build",
    "tests:regression:experiment": "npm-run-all --continue-on-error -s tests:regression:experiment:run fix-latest-report",
    "tests:regression:experiment:run": "cross-env NODE_ENV=testplan cypress run --browser chrome --env grepTags=update+refund",
    "pretests:regression:testplan": "npm run build",
    "tests:regression:testplan": "npm-run-all --continue-on-error -s tests:regression:testplan:run fix-latest-report",
    "tests:regression:testplan:run": "cross-env NODE_ENV=testplan cypress run --browser chrome --env grepTags=regression",
    "pretests:developer": "npm run build",
    "tests:developer": "cross-env NODE_ENV=testplan cypress open",
    "pretests:smoke:prod": "npm run build",
    "tests:smoke:prod": "npm-run-all --continue-on-error -s tests:smoke:prod:run fix-latest-report",
    "tests:smoke:prod:run": "cross-env NODE_ENV=production cypress run --browser chrome --env grepTags=smoke"
  },
  "license": "ISC",
  "homepage": "https://github.com/rajexcited/personal-finance-ui#readme",
  "devDependencies": {
    "@types/mochawesome": "^6.2.4",
    "@types/node": "^22.15.21",
    "cross-env": "^7.0.3",
    "cypress": "^14.5.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "dotenv": "^17.0.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@cypress/grep": "^4.1.0",
    "cypress-real-events": "^1.14.0",
    "date-and-time": "^3.6.0",
    "dotenv-flow": "^4.1.0",
    "idb": "^8.0.3",
    "uuid": "^11.1.0"
  }
}
